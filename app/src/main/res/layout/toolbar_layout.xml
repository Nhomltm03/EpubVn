<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize">

    <ImageView
        android:id="@+id/iv_back"
        android:layout_width="@dimen/dp50"
        android:layout_height="@dimen/dp50"
        android:padding="@dimen/dp5"
        android:src="@mipmap/epub"
        android:background="?android:attr/windowBackground"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="ContentDescription" />

    <EditText
        android:id="@+id/ed_find_bar"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:gravity="center|start"
        android:padding="@dimen/dp10"
        android:layout_margin="@dimen/dp5"
        android:textColor="@color/color_black"
        android:textSize="15sp"
        android:inputType="text"
        android:hint="@string/ed_find_hint"
        android:fontFamily="sans-serif-thin"
        android:background="@drawable/ed_background"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toRightOf="@id/iv_back"
        app:layout_constraintRight_toLeftOf="@id/iv_user_profile"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="Autofill,TextFields" />


    <ImageView
        android:id="@+id/iv_user_profile"
        android:layout_width="@dimen/dp50"
        android:layout_height="@dimen/dp50"
        android:layout_marginEnd="@dimen/dp5"
        android:background="?selectableItemBackgroundBorderless"
        android:tint="@color/color_gray"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toRightOf="@id/ed_find_bar"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:src="@drawable/ic_home_user"
        tools:ignore="ContentDescription" />
</androidx.constraintlayout.widget.ConstraintLayout>
